version: "3.9"
services:
  server:
    build:
      context: .
      dockerfile: apps/webhook-relay-server/Dockerfile
      args:
        - "APP=webhook-relay-server"
    ports:
      - "3000:3000"
      - "9000:9000"
    links:
      - redis
    environment:
      - "REDIS_URL=redis://redis:6379"
    depends_on:
      - redis
  client:
    build:
      context: .
      dockerfile: apps/webhook-relay-client/Dockerfile
      args:
        - "APP=webhook-relay-client"
    links:
      - server
    environment:
      - "SOURCE_URL=http://server:3000/webhooks/test/"
      - "TARGET_BASE_URL=https://jsonplaceholder.typicode.com/todos/1/"
    depends_on:
      - server
  redis:
    image: redis
